export declare enum ParticipantTaskStatus {
    created = "created",
    pending = "pending",
    completed = "completed",
    failed = "failed"
}
export declare enum ParticipantMissionStatus {
    created = "created",
    started = "started",
    completed = "completed",
    failed = "failed"
}
