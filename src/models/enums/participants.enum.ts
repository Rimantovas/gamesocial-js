export enum ParticipantTaskStatus {
  created = 'created',
  pending = 'pending',
  completed = 'completed',
  failed = 'failed',
}

export enum ParticipantMissionStatus {
  created = 'created',
  started = 'started',
  completed = 'completed',
  failed = 'failed',
}
